from decimal import Decimal

product_cost = 88.80
commission_rate = 0.08
qty = 450

#Convertir número a entero (int)
print(int(product_cost))

#Convertir número a float (float)
print(float(qty))

#Convertir número a decimal 
print(Decimal(product_cost))

